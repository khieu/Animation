package comp132.drawable;

/**
 * The Scaleable interface is implemented by
 * any shapes that are Scaleable.
 */
public interface Scaleable {
    /**
     * Scale this Object by the the specified
     * factor.
     * 
     * @param factor the factor by which this Object
     *               should be scaled.
     */
    void scale(double factor);
}
